<h1><%= @project.title %></h1>
<h3><%= @project.description %></h3>

<ul>
  <% @project.polls.each do |poll| %>
    <li>
      <div>
        <h3><%= poll.title %></h3>
        <p><strong>Description:</strong> <%= poll.description %></p>
        <p><strong>Deadline:</strong> <%= poll.deadline %></p>
        <%= link_to "Edit this poll", edit_poll_path(poll) %>
        <ul>
          <% poll.answers.each do |answer| %>
            <li><%= answer.title %> - <%= answer.votes.count %> votes</li>
          <% end %>
        </ul>
      </div>
    </li>
  <% end %>
</ul>

<%= link_to "Create new poll", new_poll_path%>
<%= link_to "Edit this project", edit_project_path %>
